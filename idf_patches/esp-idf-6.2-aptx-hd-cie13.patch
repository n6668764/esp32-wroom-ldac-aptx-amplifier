diff --git a/components/bt/host/bluedroid/common/include/common/bt_target.h b/components/bt/host/bluedroid/common/include/common/bt_target.h
--- a/components/bt/host/bluedroid/common/include/common/bt_target.h
+++ b/components/bt/host/bluedroid/common/include/common/bt_target.h
@@ -2453,6 +2453,7 @@
 /* Maximum size in bytes of the codec capabilities information element. */
 #ifndef AVDT_CODEC_SIZE
-#define AVDT_CODEC_SIZE             10
+/* aptX HD: LOSC(1) + media/codec(2) + vendor CIE(11). */
+#define AVDT_CODEC_SIZE             14
 #endif
 
 /* Maximum size in bytes of the content protection information element. */
diff --git a/components/bt/host/bluedroid/api/include/api/esp_a2dp_api.h b/components/bt/host/bluedroid/api/include/api/esp_a2dp_api.h
--- a/components/bt/host/bluedroid/api/include/api/esp_a2dp_api.h
+++ b/components/bt/host/bluedroid/api/include/api/esp_a2dp_api.h
@@ -185,5 +185,6 @@ typedef struct {
         esp_a2d_cie_m12_t   m12_info;          /*!< MPEG-1,2 audio codec capabilities */
         esp_a2d_cie_m24_t   m24_info;          /*!< MPEG-2, 4 AAC audio codec capabilities */
         esp_a2d_cie_atrac_t atrac_info;        /*!< ATRAC family codec capabilities */
+        uint8_t             vendor_info[11];   /*!< Vendor codec CIE; large enough for aptX HD */
     } cie;                                     /*!< A2DP codec information element */
 } __attribute__((packed)) esp_a2d_mcc_t;
diff --git a/components/bt/host/bluedroid/btc/profile/std/include/btc_av_api.h b/components/bt/host/bluedroid/btc/profile/std/include/btc_av_api.h
--- a/components/bt/host/bluedroid/btc/profile/std/include/btc_av_api.h
+++ b/components/bt/host/bluedroid/btc/profile/std/include/btc_av_api.h
@@ -74,3 +74,3 @@ static inline BOOLEAN btc_av_is_aptx_hd_codec_info(const UINT8 *info)
 {
-    return info != NULL && info[0] == 9 && info[2] == 0xff &&
+    return info != NULL && info[0] == 13 && info[2] == 0xff &&
            info[3] == BTC_AV_APTX_HD_VENDOR_ID0 && info[4] == BTC_AV_APTX_HD_VENDOR_ID1 &&
diff --git a/components/bt/host/bluedroid/btc/profile/std/a2dp/btc_av.c b/components/bt/host/bluedroid/btc/profile/std/a2dp/btc_av.c
--- a/components/bt/host/bluedroid/btc/profile/std/a2dp/btc_av.c
+++ b/components/bt/host/bluedroid/btc/profile/std/a2dp/btc_av.c
@@ -1550,6 +1550,8 @@ static void bte_av_media_sink_callback(tBTA_AV_EVT event, tBTA_AV_MEDIA *p_data)
 
             memset(&arg, 0, sizeof(btc_av_args_t));
             arg.mcc.type = ESP_A2D_MCT_NON_A2DP;
-            memcpy(&arg.mcc.cie, p_codec + 3, 7);
+            const size_t cie_len = p_codec[0] - 2;
+            memcpy(&arg.mcc.cie, p_codec + 3,
+                   cie_len < sizeof(arg.mcc.cie) ? cie_len : sizeof(arg.mcc.cie));
             btc_transfer_context(&msg, &arg, sizeof(btc_av_args_t), NULL, NULL);
         }
@@ -1679,8 +1681,12 @@ static void btc_av_reg_sep(uint8_t tsep, uint8_t seid, esp_a2d_mcc_t *mcc)
 #endif
         case ESP_A2D_MCT_NON_A2DP: {
             UINT8 *info = btc_av_cb.codec_caps[seid].info;
-            info[0] = 9;
+            const UINT8 *cie = (const UINT8 *)&mcc->cie;
+            const BOOLEAN hd = cie[0] == BTC_AV_APTX_HD_VENDOR_ID0 &&
+                               cie[4] == BTC_AV_APTX_HD_CODEC_ID0;
+            memset(info, 0, AVDT_CODEC_SIZE);
+            info[0] = hd ? 13 : 9;
             info[1] = A2D_MEDIA_TYPE_AUDIO << 4;
             info[2] = ESP_A2D_MCT_NON_A2DP;
-            memcpy(info + 3, &mcc->cie, 7);
+            memcpy(info + 3, &mcc->cie, hd ? 11 : 7);
             if (!btc_av_is_aptx_codec_info(info)) {
