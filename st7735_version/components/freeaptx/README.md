# freeaptX decoder

This component vendors the `freeaptx` decoder used for aptX Classic playback.
The implementation is licensed under LGPL-2.1-or-later; see `COPYING` and the
copyright header in `freeaptx.c`.

Upstream: https://github.com/pali/libfreeaptx (ESP-IDF packaging reference:
https://github.com/cfint/libfreeaptx-esp)

